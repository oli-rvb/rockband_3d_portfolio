// Draco decoder served from public/draco (copied from three/examples/jsm/libs/draco/gltf).
// Self-hosted so model decoding never depends on a third-party CDN and can be preloaded.
export const DRACO_DECODER_PATH = `${import.meta.env.BASE_URL}draco/`
